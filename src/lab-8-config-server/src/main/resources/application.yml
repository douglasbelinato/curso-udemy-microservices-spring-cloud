---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/douglasbelinato/curso-udemy-microservices-spring-cloud
          searchPaths: configdata
server:
  port: 8001
# For simplicity, disable security when posting to bus/refresh:  
management:
  security:
    enabled: false